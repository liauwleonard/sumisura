.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.env
# Anchored: an unanchored `data/` also matches `src/data/`, which silently kept the
# measurement and mannequin definitions out of the repo and broke the CI build.
/data/

node_modules/
dist/
*.tsbuildinfo
.DS_Store
